#include <ExWindow/ExWindow.h>
#include "ui_ExWindow.h"

ExWindow::ExWindow(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::ExWindow)
{
    ui->setupUi(this);
}

ExWindow::~ExWindow()
{
    delete ui;
}
