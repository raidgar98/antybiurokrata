#pragma once

#include <QtWidgets/QDialog>

namespace Ui { class ExWindow; }

class ExWindow : public QDialog
{
    Q_OBJECT

public:
    explicit ExWindow(QWidget *parent = nullptr);
    ~ExWindow();

private:
    Ui::ExWindow *ui;
};